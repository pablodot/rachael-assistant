# browser-agent environment variables
# Copy this file to .env and adjust as needed.
# All variables are prefixed with BROWSER_.

# ── Server ──────────────────────────────────────────────────────────────────
# Bind address. Use 127.0.0.1 (default) to restrict to localhost only.
BROWSER_HOST=127.0.0.1
BROWSER_PORT=8001

# ── Playwright ───────────────────────────────────────────────────────────────
# Path to the dedicated Chromium profile directory.
BROWSER_CHROMIUM_PROFILE_DIR=./chromium-profile

# Run Chromium headless? false = show browser window (useful for debugging).
BROWSER_HEADLESS=false

# Milliseconds of delay between Playwright actions (helps with stability).
BROWSER_SLOW_MO=100

# ── Security ─────────────────────────────────────────────────────────────────
# Comma-separated list of allowed domains.
# Leave empty to allow all domains (development mode).
# Example: BROWSER_DOMAIN_ALLOWLIST=google.com,github.com,wikipedia.org
BROWSER_DOMAIN_ALLOWLIST=

# Maximum number of browser actions per task before the session is rejected.
BROWSER_MAX_STEPS_PER_TASK=50

# Comma-separated keywords in element text/selectors that trigger a stop-point.
# Matching elements require force=true (i.e. explicit user approval) to proceed.
BROWSER_STOP_POINT_KEYWORDS=checkout,payment,pay now,confirm purchase,place order,buy now,complete order,submit order,delete,remove account,unsubscribe,cancel subscription
